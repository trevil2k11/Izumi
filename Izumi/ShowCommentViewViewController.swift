//
//  ShowCommentViewViewController.swift
//  Izumi
//
//  Created by Ilya Kosolapov on 10.11.16.
//  Copyright © 2016 Ilya Kosolapov. All rights reserved.
//

import UIKit

class ShowCommentViewViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    @IBAction func sendComment(_ sender: UIButton) {
//        let body:String = jsonLib.returnAddComment(helperLib.loadUserDefaults("user_id"), pic_id: String(pic_id), rating: String(hiddenRating.selectedSegmentIndex), comment: helperLib.specUnicodeCyrillic(hiddenCommentText.text! as NSString) as String)
//        self.hiddenCommentView.isHidden = true;
//        self.curlSender.sendData(body) {result in
//            self.getComment(String(self.pic_id))
//        }
    }
}
